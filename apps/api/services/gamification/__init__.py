"""Gamification service package — Phase 16c Bundle C (Badge backend).

Sub-package houses badge catalog/service code. Other gamification
services (xp, streak, freeze) currently live at the top-level
``services/`` namespace; this sub-package is opened by Bundle C and
will host future badge-adjacent modules.
"""
