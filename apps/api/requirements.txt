# Core
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.19
pydantic>=2.11.0,<3.0.0
pydantic-settings>=2.7.1,<3.0.0

# Database
sqlalchemy[asyncio]==2.0.47
asyncpg==0.30.0
alembic==1.14.1
pgvector==0.3.6

# Redis
redis==5.2.1

# LLM Providers
openai==1.65.2
anthropic==0.46.0

# File Parsing — Unified content extraction
# Primary: Crawl4AI (web + PDF + HTML)
crawl4ai==0.4.0
# HTML fallback (pinned to crawl4ai compat range)
lxml>=5.3,<6.0

# Office formats — optional; code falls back to python-docx/pptx if missing
# Install separately: pip install unstructured langchain-community
beautifulsoup4==4.12.3
# Legacy extractors (kept for fallback)
trafilatura==2.0.0
python-pptx==1.0.2
python-docx==1.1.2
# MIME detection (3-tier: filetype → python-magic → extension)
filetype==1.2.0
python-magic==0.4.27
# Optional heavy dependency for high-quality PDF parsing:
# marker-pdf==1.6.2

# Token counting & content trimming
tiktoken==0.7.0

# Content dedup (xxhash ~10x faster than SHA-256)
xxhash==3.6.0

# Content Classification
thefuzz==0.22.1

# Browser Automation (optional)
scrapling>=0.2
playwright>=1.49.0

# Canvas LMS (optional)
canvasapi>=3.3.0

# Workflow Engine
langgraph>=0.2.0

# Scheduler
apscheduler>=3.10.0

# Authentication
bcrypt>=4.0.0
python-jose[cryptography]>=3.3.0

# Web Push Notifications
pywebpush>=2.0.0
py-vapid>=1.9.0

# YAML parsing (skills system frontmatter)
pyyaml>=6.0

# Utilities
tenacity==9.0.0
httpx==0.28.1
python-dotenv==1.0.1
sse-starlette==2.2.1

# Testing (dev)
pytest-asyncio>=0.23.0

# Embeddings (optional: for local embeddings without OpenAI)
# sentence-transformers>=2.2.0
